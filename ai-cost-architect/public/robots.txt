User-agent: *
Allow: /

# The app is client-rendered: a crawler that cannot fetch the JS and CSS bundles
# sees an empty <app-root> shell. Never disallow *.js / *.css here.

Sitemap: https://tokiq.in/sitemap.xml
